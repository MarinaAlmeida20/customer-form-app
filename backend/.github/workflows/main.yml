name: Deploy

on:
  push:
    branches:
      - master

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "customer-form-app-server" #Must be unique in Heroku
          heroku_email: "marinabrito.ma@gmail.com"
          buildpack: "https://github.com/HashNuke/heroku-buildpack-elixir.git"
          appdir: "backend" # <- This will point to the api folder in your project


  # frontend:
  #   needs: backend
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
  #       with:
  #         heroku_api_key: ${{secrets.HEROKU_API_KEY}}
  #         heroku_app_name: "customer-form-app-server" #Must be unique in Heroku
  #         heroku_email: "marinabrito.ma@gmail.com"
  #         appdir: "frontend" # <- This will point to the api folder in your project
